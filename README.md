# SOP LLM Service

Асинхронный сервис для работы с LLM моделями и провайдерами.

## Описание

SOP LLM Service - это современный асинхронный сервис на FastAPI для работы с различными языковыми моделями (LLM). Проект следует принципам async-first архитектуры и стандартам wiki-engine.

## Основные возможности

- **Множественные провайдеры**: Поддержка локальных моделей (HuggingFace), Claude API, LM Studio
- **Embedding модели**: Генерация векторных представлений текста
- **JSON Fixer**: Автоматическое исправление невалидного JSON от LLM
- **Кэширование**: Redis для кэширования результатов
- **Мониторинг**: WebSocket для real-time статистики
- **Async-first**: Полностью асинхронная архитектура

## Структура проекта

```
sop_llm/
├── config/              # Конфигурационные файлы (YAML)
├── scripts/             # Скрипты для развертывания
├── src/
│   ├── api/            # API endpoints
│   ├── core/           # Основная конфигурация
│   ├── modules/        # Бизнес-логика модулей
│   │   ├── llm/       # LLM провайдеры и сервисы
│   │   └── monitoring/ # Мониторинг
│   ├── shared/         # Общий код
│   │   ├── cache/     # Redis кэш
│   │   ├── errors/    # Обработка ошибок
│   │   ├── logging/   # Логирование
│   │   └── utils/     # Утилиты
│   └── tests/         # Тесты
├── docker-compose.yml
├── Makefile
└── pyproject.toml
```

## Требования

- Python 3.11+
- Redis 7.0+
- CUDA (опционально, для GPU)

## Установка

```bash
# Клонирование репозитория
git clone git@github.com:VladPil/sop_llm.git
cd sop_llm

# Создание виртуального окружения
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или .venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -e ".[dev]"

# Копирование примера конфигурации
cp .env.example .env
```

## Конфигурация

Настройте переменные окружения в `.env`:

```env
# Основные настройки
APP_NAME="SOP LLM Service"
ENVIRONMENT=development
DEBUG=true

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# LLM модели
LLM_DEFAULT_MODEL="Qwen/Qwen2.5-7B-Instruct"
LLM_DEFAULT_EMBEDDING_MODEL="intfloat/multilingual-e5-large"
```

## Запуск

### Development

```bash
# Запуск через uvicorn
make run

# Или напрямую
python -m src.main
```

### Docker

```bash
# Запуск всех сервисов
docker-compose up -d

# Логи
docker-compose logs -f app
```

## API Документация

После запуска сервиса документация доступна по адресам:

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
- OpenAPI JSON: http://localhost:8000/openapi.json

## Основные endpoints

- `POST /api/v1/generate` - Генерация текста
- `POST /api/v1/embedding` - Генерация embedding
- `POST /api/v1/similarity` - Вычисление схожести текстов
- `POST /api/v1/tasks` - Создание задач
- `GET /api/v1/health` - Health check
- `WS /ws/monitoring` - WebSocket мониторинг

## Тестирование

```bash
# Запуск всех тестов
make test

# Запуск с coverage
make test-cov

# Запуск конкретной категории
pytest -m unit
pytest -m integration
```

## Разработка

```bash
# Линтинг и форматирование
make lint
make format

# Проверка типов
make typecheck
```

## Лицензия

MIT License

## Авторы

- Vladislav <vladislav@example.com>

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)
